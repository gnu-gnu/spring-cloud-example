#client-service-2 implements Config-first-bootstrap
#it binds to spring.cloud.config.uri
spring:
  application:
    name: client-service-2
  cloud:
    config:
      uri:
      - http://localhost:9000/