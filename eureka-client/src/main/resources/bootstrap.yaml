# to fetch configurations from the remote config server, application.yaml is replaced by bootstrap.yaml
# bootstrap tries to get configurations from remote server matches with spring.application.name.
spring:
  application:
    name: client-service
  cloud:
    config:
      uri:
      - http://localhost:9000/